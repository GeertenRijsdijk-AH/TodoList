This is a learning project, implementing a full stack 'TODO list' web application.
It consists of:
- A Next.js frontend (using TypeScript)
- A FastAPI backend (using Python)
- A Postgres database
All of these components are containerized using Docker.

To run:
- make run